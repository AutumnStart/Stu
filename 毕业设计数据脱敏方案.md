# 毕业设计数据脱敏与模拟数据方案

## 🔒 需要修改的敏感数据部分

### 1. 配置文件敏感信息

**文件：`feishu_config.json`**
```json
{
  "app_id": "cli_xxxxxxxxxx",  // 替换为模拟ID
  "app_secret": "xxxxxxxxxxxx", // 替换为模拟密钥
  "user_access_token": "u-xxxxxxxxxx", // 替换为模拟token
  "spreadsheet_token": "xxxxxxxxxx", // 替换为模拟表格ID
  "sheet_id": "xxxxxxxxxx" // 替换为模拟工作表ID
}
```

**修改方案：**
- 创建 `feishu_config_demo.json` 作为演示配置
- 所有敏感字段使用 `DEMO_` 前缀
- 在代码中添加演示模式开关

### 2. API密钥和访问凭证

**文件：`Tool/audience_mismatch_analysis.py`, `agents/agent10.py`**
```python
# 当前代码
genai.configure(api_key="your-actual-api-key")

# 修改为
api_key = os.getenv('GEMINI_API_KEY', 'DEMO_API_KEY_FOR_GRADUATION_PROJECT')
genai.configure(api_key=api_key)
```

### 3. 真实素材数据

**需要脱敏的文件：**
- `data/素材数据.xlsx`
- `json/素材数据分析/素材数据.json`
- `data/素材ID.json`
- `screenshots/` 目录下的所有截图

## 📊 模拟数据生成方案

### 1. 素材基础数据模拟

**创建模拟数据生成器：`generate_demo_data.py`**
```python
import pandas as pd
import numpy as np
import json
from datetime import datetime, timedelta
import random

def generate_material_data(num_materials=100):
    """生成模拟素材数据"""
    materials = []
    
    # 模拟素材类型
    material_types = ['视频', '图片', '轮播图']
    industries = ['美妆', '服装', '数码', '食品', '家居']
    
    for i in range(num_materials):
        material_id = f"DEMO_{7000000000000000000 + i}"
        
        # 基础信息
        material = {
            '素材ID': material_id,
            '素材名称': f'演示素材_{i+1}',
            '素材类型': random.choice(material_types),
            '行业分类': random.choice(industries),
            '创建时间': (datetime.now() - timedelta(days=random.randint(1, 365))).strftime('%Y-%m-%d'),
            
            # 投放数据（模拟真实分布）
            '消耗': round(np.random.lognormal(8, 1.5), 2),  # 对数正态分布，模拟消耗分布
            '展现': random.randint(10000, 1000000),
            '点击': random.randint(100, 50000),
            '转化': random.randint(10, 1000),
            
            # 效果指标
            'CPM': round(random.uniform(5, 50), 2),
            'CTR': round(random.uniform(0.5, 8.0), 3),
            '转化率': round(random.uniform(0.1, 5.0), 3),
            '3秒播放率': round(random.uniform(20, 80), 2),
            
            # 人群数据
            '男性占比': round(random.uniform(20, 80), 1),
            '女性占比': round(random.uniform(20, 80), 1),
            '18-24岁占比': round(random.uniform(10, 40), 1),
            '25-34岁占比': round(random.uniform(20, 50), 1),
            '35-44岁占比': round(random.uniform(15, 35), 1),
            '45岁以上占比': round(random.uniform(5, 25), 1),
        }
        
        # 确保百分比合理
        total_gender = material['男性占比'] + material['女性占比']
        material['男性占比'] = round(material['男性占比'] / total_gender * 100, 1)
        material['女性占比'] = round(100 - material['男性占比'], 1)
        
        materials.append(material)
    
    return pd.DataFrame(materials)

def generate_behavior_data(material_ids):
    """生成行为分析数据"""
    behavior_data = []
    
    for material_id in material_ids:
        # 模拟视频播放行为节点
        total_views = random.randint(1000, 100000)
        
        behavior = {
            '素材ID': material_id,
            '总播放量': total_views,
            '3秒播放量': int(total_views * random.uniform(0.4, 0.8)),
            '25%播放量': int(total_views * random.uniform(0.3, 0.6)),
            '50%播放量': int(total_views * random.uniform(0.2, 0.4)),
            '75%播放量': int(total_views * random.uniform(0.1, 0.3)),
            '完整播放量': int(total_views * random.uniform(0.05, 0.2)),
            '点击量': random.randint(50, 5000),
            '分享量': random.randint(10, 500),
            '评论量': random.randint(5, 200),
        }
        
        behavior_data.append(behavior)
    
    return behavior_data
```

### 2. 图像数据模拟

**创建模拟截图生成器：`generate_demo_screenshots.py`**
```python
import matplotlib.pyplot as plt
import numpy as np
from PIL import Image, ImageDraw, ImageFont
import os

def generate_demo_chart(material_id, chart_type='流失数'):
    """生成模拟数据图表"""
    fig, ax = plt.subplots(figsize=(10, 6))
    
    if chart_type == '流失数':
        # 模拟流失漏斗数据
        stages = ['展现', '3秒播放', '25%播放', '50%播放', '75%播放', '完整播放']
        values = [100000, 65000, 45000, 28000, 15000, 8000]  # 模拟递减数据
        
        ax.bar(stages, values, color=['#FF6B6B', '#4ECDC4', '#45B7D1', '#96CEB4', '#FFEAA7', '#DDA0DD'])
        ax.set_title(f'素材 {material_id} - 播放流失分析', fontsize=14, fontweight='bold')
        ax.set_ylabel('用户数量')
        
    elif chart_type == '点击次数':
        # 模拟点击时间分布
        hours = list(range(24))
        clicks = np.random.poisson(100, 24)  # 泊松分布模拟点击
        
        ax.plot(hours, clicks, marker='o', linewidth=2, markersize=6)
        ax.set_title(f'素材 {material_id} - 24小时点击分布', fontsize=14, fontweight='bold')
        ax.set_xlabel('小时')
        ax.set_ylabel('点击次数')
        ax.grid(True, alpha=0.3)
    
    plt.tight_layout()
    
    # 保存到对应目录
    save_dir = f'screenshots_demo/Material_{material_id}'
    os.makedirs(save_dir, exist_ok=True)
    
    timestamp = datetime.now().strftime('%Y%m%d_%H%M%S')
    filename = f'{chart_type}_{timestamp}.png'
    filepath = os.path.join(save_dir, filename)
    
    plt.savefig(filepath, dpi=150, bbox_inches='tight')
    plt.close()
    
    return filepath
```

### 3. 视频素材模拟

**创建模拟视频生成器：`generate_demo_videos.py`**
```python
import cv2
import numpy as np
from PIL import Image, ImageDraw, ImageFont

def create_demo_video(material_id, duration=15):
    """创建演示用的模拟视频"""
    
    # 视频参数
    width, height = 720, 1280  # 竖屏格式
    fps = 30
    total_frames = duration * fps
    
    # 创建视频写入器
    fourcc = cv2.VideoWriter_fourcc(*'mp4v')
    video_path = f'storage/video_demo/{material_id}.mp4'
    os.makedirs(os.path.dirname(video_path), exist_ok=True)
    
    out = cv2.VideoWriter(video_path, fourcc, fps, (width, height))
    
    # 生成视频帧
    for frame_num in range(total_frames):
        # 创建彩色背景
        hue = (frame_num * 2) % 360
        frame = np.full((height, width, 3), [hue//2, 100, 200], dtype=np.uint8)
        frame = cv2.cvtColor(frame, cv2.COLOR_HSV2BGR)
        
        # 添加文字
        text = f'演示素材 {material_id}'
        font = cv2.FONT_HERSHEY_SIMPLEX
        cv2.putText(frame, text, (50, height//2), font, 2, (255, 255, 255), 3)
        
        # 添加时间戳
        time_text = f'{frame_num//fps:02d}s'
        cv2.putText(frame, time_text, (width-150, 50), font, 1, (255, 255, 255), 2)
        
        out.write(frame)
    
    out.release()
    return video_path
```

## 🛠️ 代码修改清单

### 1. 添加演示模式开关

**创建：`config/demo_config.py`**
```python
class DemoConfig:
    DEMO_MODE = True  # 演示模式开关
    
    # 演示用的API配置
    DEMO_API_KEYS = {
        'gemini': 'DEMO_GEMINI_API_KEY',
        'feishu_app_id': 'DEMO_APP_ID',
        'feishu_secret': 'DEMO_APP_SECRET'
    }
    
    # 数据文件路径映射
    DATA_PATHS = {
        'materials': 'data_demo/素材数据_演示.xlsx' if DEMO_MODE else 'data/素材数据.xlsx',
        'screenshots': 'screenshots_demo/' if DEMO_MODE else 'screenshots/',
        'videos': 'storage/video_demo/' if DEMO_MODE else 'storage/video/'
    }
```

### 2. 修改主要处理文件

**修改：`Tool/integrated_material_processor.py`**
```python
# 在文件开头添加
from config.demo_config import DemoConfig

# 修改数据读取部分
def load_material_data():
    if DemoConfig.DEMO_MODE:
        return pd.read_excel(DemoConfig.DATA_PATHS['materials'])
    else:
        return pd.read_excel('data/素材数据.xlsx')
```

### 3. 创建演示数据初始化脚本

**创建：`setup_demo_environment.py`**
```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
毕业设计演示环境初始化脚本
生成所有必要的模拟数据和配置文件
"""

import os
import json
from generate_demo_data import generate_material_data, generate_behavior_data
from generate_demo_screenshots import generate_demo_chart
from generate_demo_videos import create_demo_video

def setup_demo_environment():
    """初始化演示环境"""
    print("🚀 开始初始化毕业设计演示环境...")
    
    # 1. 创建演示目录
    demo_dirs = [
        'data_demo',
        'screenshots_demo', 
        'storage/video_demo',
        'json_demo/素材数据分析',
        'config'
    ]
    
    for dir_path in demo_dirs:
        os.makedirs(dir_path, exist_ok=True)
        print(f"✅ 创建目录: {dir_path}")
    
    # 2. 生成模拟素材数据
    print("📊 生成模拟素材数据...")
    df_materials = generate_material_data(50)  # 生成50个模拟素材
    df_materials.to_excel('data_demo/素材数据_演示.xlsx', index=False)
    
    # 3. 生成JSON格式数据
    material_ids = df_materials['素材ID'].tolist()
    json_data = df_materials.to_dict('records')
    
    with open('json_demo/素材数据分析/素材数据.json', 'w', encoding='utf-8') as f:
        json.dump(json_data, f, ensure_ascii=False, indent=2)
    
    # 4. 生成行为分析数据
    behavior_data = generate_behavior_data(material_ids[:10])  # 前10个素材
    with open('data_demo/行为分析数据.json', 'w', encoding='utf-8') as f:
        json.dump(behavior_data, f, ensure_ascii=False, indent=2)
    
    # 5. 生成模拟截图
    print("📸 生成模拟数据图表...")
    for i, material_id in enumerate(material_ids[:10]):
        generate_demo_chart(material_id, '流失数')
        if i % 2 == 0:  # 一半素材生成点击数据
            generate_demo_chart(material_id, '点击次数')
        print(f"✅ 生成图表: {material_id}")
    
    # 6. 创建演示配置文件
    demo_config = {
        "app_id": "DEMO_APP_ID_FOR_GRADUATION",
        "app_secret": "DEMO_SECRET_FOR_GRADUATION", 
        "user_access_token": "DEMO_TOKEN_FOR_GRADUATION",
        "spreadsheet_token": "DEMO_SPREADSHEET_FOR_GRADUATION",
        "sheet_id": "DEMO_SHEET_FOR_GRADUATION"
    }
    
    with open('feishu_config_demo.json', 'w', encoding='utf-8') as f:
        json.dump(demo_config, f, ensure_ascii=False, indent=2)
    
    # 7. 创建README说明
    readme_content = """
# 毕业设计演示环境说明

## 📋 演示数据说明

本项目为毕业设计演示版本，所有数据均为模拟生成，不涉及任何真实商业数据。

### 🔧 演示环境特点

1. **数据安全**: 所有敏感信息已脱敏处理
2. **功能完整**: 保留所有核心功能和分析能力  
3. **真实模拟**: 数据分布符合真实业务场景
4. **可重现**: 支持重新生成演示数据

### 📊 演示数据包含

- 50个模拟素材的完整数据
- 10个素材的详细行为分析
- 对应的数据可视化图表
- 完整的配置文件模板

### 🚀 快速开始

```bash
# 初始化演示环境
python setup_demo_environment.py

# 运行演示分析
python run_demo_analysis.py
```

### ⚠️ 注意事项

- 本演示版本仅用于学术展示
- 所有API密钥均为演示用途
- 真实部署需要配置实际的API凭证
"""
    
    with open('README_DEMO.md', 'w', encoding='utf-8') as f:
        f.write(readme_content)
    
    print("\n🎉 演示环境初始化完成!")
    print("\n📁 生成的演示文件:")
    print("  - data_demo/素材数据_演示.xlsx (模拟素材数据)")
    print("  - screenshots_demo/ (模拟截图)")
    print("  - feishu_config_demo.json (演示配置)")
    print("  - README_DEMO.md (演示说明)")
    print("\n✅ 现在可以安全地用于毕业设计展示了!")

if __name__ == '__main__':
    setup_demo_environment()
```

## 📝 使用建议

### 1. 答辩展示策略

- **强调技术实现**: 重点展示AI分析、自动化流程等技术亮点
- **数据脱敏说明**: 主动说明数据安全处理措施
- **功能演示**: 现场运行演示版本，展示完整功能
- **对比分析**: 展示传统方式vs AI驱动方式的效果对比

### 2. 文档完善

- 添加数据脱敏处理的技术文档
- 补充模拟数据生成的算法说明
- 完善系统架构和技术选型的理论依据
- 增加性能测试和效果评估报告

### 3. 代码规范

- 添加详细的代码注释
- 完善异常处理和日志记录
- 增加单元测试和集成测试
- 优化代码结构和模块化设计

## 🎯 总结

通过以上数据脱敏和模拟数据方案，你的项目可以:

✅ **完全消除数据安全风险**
✅ **保持所有核心功能完整**
✅ **提供真实的演示效果**
✅ **符合学术规范要求**
✅ **支持现场演示和测试**

这样既保护了商业数据的安全性，又确保了毕业设计的完整性和可展示性。